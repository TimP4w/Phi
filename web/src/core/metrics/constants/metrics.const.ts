/** Kinds that show metrics chips/tabs (spec: tree sparkline whitelist). */
export const METRICS_KINDS = new Set([
  "Pod",
  "Deployment",
  "StatefulSet",
  "DaemonSet",
  "Kustomization",
  "HelmRelease",
]);
